using System.IO;
using System;

class Program
{
    static void Main()
    {
        int result;
       int TryParse("asdbh", out resul); 
       if(result==0)
       {
           console.WriteLine("Not a num");
       }
       else{
           console.WriteLine("its a num");
       }
        Console.WriteLine(result);
        Console.ReadLine();
    }
}